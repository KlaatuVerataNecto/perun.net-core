//using ImageSharp;
//using infrastucture.libs.cryptography;
using System;
//using System.Drawing;
//using System.Drawing.Imaging;
using System.IO;

namespace infrastucture.libs.image
{
    public static class ImageService
    {
        public static void Save(FileStream fileStream, string pathToImages, Guid custom_id)
        {
                //using (FileStream output = File.OpenWrite(@"C:\test.jpg"))
                //using (Image<Rgba32> image = Image.Load<Rgba32>(fileStream))
                //{
                //    image.Resize(image.Width / 2, image.Height / 2)
                //         .Grayscale()
                //         .Save(output);
                //}
        }
    }
}
